import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#4648ba',
    },
    secondary: {
      main: '#f3bc77',
    }
  },
  typography: {
    fontFamily: "Lora, sans-serif",
    fontSize: 16
  }
});
